export type QueryBuilderOptions = {
  /** The GraphQL server endpoint. Defaults to `https://arweave-search.goldsky.com/graphql`. */
  server_url?: string;

  /**
   * @example
   * ```
   * ```
   */
  operation?: string;

  return_schema?: string;
};
